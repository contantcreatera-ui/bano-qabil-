# Bano Qabil Hyderabad Chapter
# This client-side application runs with zero required environment variables.
# All admissions registry operations, course data, and UI workflows operate locally without requiring third-party API keys or external secrets.
# Optional: External or custom backend API endpoint for course catalog (defaults to /api/courses.json)
VITE_COURSES_API_URL=
